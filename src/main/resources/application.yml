server:
  port: 8082

spring:
  datasource:
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    url: jdbc:hsqldb:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    root: INFO
    com:
      zaxxer:
        hikari:
          pool:
            HikariPool: INFO